version: '3'
services:
  redis-server: 
    image: 'redis'
    command: ["redis-server", "--bind", "redis", "--port", "6379"]

    ports:
      - "6379:6379"
  node-app:
    build: .
    ports:
      - "5001:5001"